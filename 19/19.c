#include<stdio.h>
int main(){
    int n;
    printf("Enter the range till which the sum of Natural Numbers need to be printed: ");
    int sum_of_Natural = (n*(n+1))/2;
    printf("The sum of %d Natural numbers is %d",n,sum_of_Natural);
}